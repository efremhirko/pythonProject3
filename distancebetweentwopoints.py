import math
# Given points
x0, y0 = 2, 2
x1, y1 = 5, 6

distance = math.sqrt((x1 - x0)**2 + (y1 - y0)**2)

print(f"The distance between the points ({x0},{y0}) and ({x1},{y1}) is {distance:.2f}")

# In this code:
#
# x0, y0 are the coordinates of the first point (2,2),
# x1, y1 are the coordinates of the second point (5,6),
# The distance is calculated using the given formula, and
# The print statement displays the calculated distance.
print("\nstudent name: Ephrem Hirko\n")
print("ID: RU1360/12")